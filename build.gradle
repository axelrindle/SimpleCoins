group 'de.lalo5.simplecoins'
version '1.0'

repositories {
    mavenCentral()
}

apply plugin: 'java'

sourceSets {
    main.java.srcDir("src/main")
    test.java.srcDir("src/test")
}

libsDirName = '../../../../Desktop/Minecraft/MC Localhost/plugins'

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'

    compile fileTree(dir: 'libs', include: '*.jar')
}

task runCraftBukkit(type:Exec) {
    doFirst {
        println "Starting Spigot server..."
        workingDir = file('C:/Users/Axel/Desktop/Minecraft/MC Localhost/')
        commandLine = ['cmd', '/C', 'start', 'start.bat']
    }
}
jar.finalizedBy runCraftBukkit